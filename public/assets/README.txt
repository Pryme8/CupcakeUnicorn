Drop art assets in this folder so Phaser can load them by URL.

Expected filenames (update in src/game/assets.ts if you rename):
- unicorn.png (sprite sheet)
- rainbow-cloud.png

When ready to use art:
1) Update frame sizes in src/game/assets.ts
2) Set USE_ASSETS to true in src/game/assets.ts
